# 5.1 表达式语句



```javascript
greeting = "Hello" + name;
i *= 3;
```

```javascript
counter++;
```

```javascript
delete o.x;
```





# 5.2 复合语句与空语句

​                                                                                                                                                                                                                               





# 5.3 条件语句



## 1. if





## 2. else if

```javascript

```



## 3. switch







# 5.4 循环语句



## 1. while

​                                                                                                                   



## 2. do/while





## 3. for





## 4. for/of

**`下面这个例子演示了如何迭代一个数值数组并计算所有数值之和：`**

```javascript
let data = [1, 2, 3, 4, 5, 6, 7, 8, 9], sum = 0;

for (let element of data) {
    sum += element;
}

sum; // 45
```





### for/of 与对象

**`对象（默认）是不可迭代的。运行时尝试对常规对象使用 for/of 会抛出 TypeError：`**

```javascript
let o = { x: 1, y: 2, z: 3 };

for(let element of o) { // 抛出 TypeError，因为 o 不是可迭代对象
    console.log(element);
}
```



**`如果想迭代对象的属性，可以使用 for/in 循环，或者基于 Object.keys() 方法的结果使用 for/of：`**

```javascript
let o = { x: 1, y: 2, z: 3 };

let keys = "";

for (let k of Object.keys(o)) {
    keys += k;
}

keys; // "xyz"
```



**`如果你不在乎对象的键，也可以像下面这样迭代每个键对应的值：`** 

```javascript
let sum = 0;

for(let v of Object.values(o)) {
    sum += v;
}

sum; // 6
```



**`如果你既想要对象属性的键，也想要属性的值，可以基于 Object.entries() 和解构赋值来使用 for/of：`**

```javascript
let pairs = "";

for(let [k, v] of Object.entries(o)) {
    pairs += k + v;
}

pairs; // "x1y2z3"
```





### for/of 与字符串

```javascript
let frequency = {};

for(let letter of "mississippi") {
    if (frequency[letter]) {
        frequency[letter]++;
    } else {
        frequency[letter] = 1;
    }
}

frequency; // { m: 1, i: 4, s: 4, p: 2 }                                         
```





### for/of 与 Set 和 Map

```javascript
let text = "Na na na na na na na na Batman!";

let wordSet = new Set(text.split(" "));

let unique = [];

for(let word of wordSet) {
    unique.push(word);
}

unique; // ["Na", "na", "Batman!"]
```



```javascript
let m = new Map([[1, "one"]]);

for(let [key, value] of m) {
    key; // 1
    value; // "one"
}
```







​                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                

​                                    

​                           

​                                                                                                                                                                                                                                                                                                                                                                                      