# 1. Number()、parseInt()、parseFloat()

## 核心记忆原则

### 1. Number() - 最严格的转换
**记忆口诀："要么全对，要么NaN"**

- 空字符串 → 0
- 纯数字字符串 → 对应数字
- 任何包含非数字字符的字符串 → NaN
- null → 0
- undefined → NaN
- 布尔值：true → 1, false → 0

```javascript
Number("123")     // 123
Number("123abc")  // NaN (有非数字字符)
Number("")        // 0 (空字符串特例)
Number("  ")      // 0 (空白字符串)
```

### 2. parseInt() - 从左到右解析整数
**记忆口诀："能解析多少是多少，遇到非数字就停"**

- 从字符串开头开始解析
- 遇到第一个非数字字符就停止
- 如果开头就不是数字，返回NaN
- 可以指定进制（第二个参数）

```javascript
parseInt("123abc")    // 123 (解析到abc就停)
parseInt("abc123")    // NaN (开头不是数字)
parseInt("12.34")     // 12 (遇到小数点停止)
parseInt("10", 2)     // 2 (二进制解析)
```

### 3. parseFloat() - 从左到右解析浮点数
**记忆口诀："parseInt的浮点版本"**

- 类似parseInt，但可以解析小数点
- 只能解析十进制（没有第二个参数）
- 遇到第二个小数点或非数字字符停止

```javascript
parseFloat("12.34abc")  // 12.34
parseFloat("12.34.56")  // 12.34 (第二个小数点停止)
parseFloat("abc12.34")  // NaN (开头不是数字)
```

## 实用记忆技巧

### 1. 严格程度排序
```
Number() > parseFloat() > parseInt()
(最严格)              (最宽松)
```

### 2. 使用场景记忆
- **Number()**: 确保输入完全是数字时使用
- **parseInt()**: 提取字符串开头的整数部分
- **parseFloat()**: 提取字符串开头的数字部分（包含小数）

### 3. 边界情况记忆表

| 输入      | Number() | parseInt() | parseFloat() |
| --------- | -------- | ---------- | ------------ |
| ""        | 0        | NaN        | NaN          |
| "  "      | 0        | NaN        | NaN          |
| "123"     | 123      | 123        | 123          |
| "123abc"  | NaN      | 123        | 123          |
| "abc123"  | NaN      | NaN        | NaN          |
| "12.34"   | 12.34    | 12         | 12.34        |
| null      | 0        | NaN        | NaN          |
| undefined | NaN      | NaN        | NaN          |

### 4. 实践建议

1. **多练习边界情况**：空字符串、null、undefined等
2. **记住特殊规则**：Number()对空字符串返回0
3. **理解解析vs转换**：parseInt/parseFloat是解析，Number是转换
4. **使用console测试**：遇到不确定的情况就测试一下

通过这些记忆方法和规律，你就能更好地掌握这三个函数的使用了！

<br>

# 2. Boolean()、isFinite()、Number.isFinite()、isNaN()、Number.isNaN()

## 1. Boolean() - 转换为布尔值

**记忆口诀：「假值六兄弟，其余都是真」**

假值（falsy）只有6个：
- `false`
- `0` 和 `-0`
- `""` 空字符串
- `null`
- `undefined`
- `NaN`

```javascript
Boolean(0)        // false
Boolean("")       // false
Boolean(null)     // false
Boolean(undefined) // false
Boolean(NaN)      // false
Boolean(false)    // false

// 其他所有值都是 true
Boolean("0")      // true (字符串"0"不是空字符串)
Boolean([])       // true (空数组也是对象)
Boolean({})       // true (空对象)
```

## 2. isFinite() vs Number.isFinite()

**记忆技巧：「全局宽松，静态严格」**

- `isFinite()` - 全局函数，会先转换类型
- `Number.isFinite()` - 静态方法，不转换类型

```javascript
// 全局 isFinite() 会先转换
isFinite("123")     // true (先转换为数字123)
isFinite("abc")     // false (转换为NaN)

// Number.isFinite() 严格判断
Number.isFinite("123") // false (字符串不是数字)
Number.isFinite(123)   // true
Number.isFinite(Infinity) // false
```

## 3. isNaN() vs Number.isNaN()

**记忆技巧：「全局会转换，静态要精确」**

- `isNaN()` - 全局函数，会先尝试转换为数字
- `Number.isNaN()` - 静态方法，只有真正的NaN才返回true

```javascript
// 全局 isNaN() 会先转换
isNaN("abc")        // true ("abc"转换为NaN)
isNaN("123")        // false ("123"转换为123)
isNaN(undefined)    // true (undefined转换为NaN)

// Number.isNaN() 严格判断
Number.isNaN("abc")     // false ("abc"不是NaN类型)
Number.isNaN(NaN)       // true (只有真正的NaN)
Number.isNaN(undefined) // false
```

## 记忆总结

### 规律记忆法：
1. **全局函数** (`isFinite`, `isNaN`) = 宽松模式，会类型转换
2. **Number静态方法** (`Number.isFinite`, `Number.isNaN`) = 严格模式，不转换
3. **Boolean()** = 只记住6个假值，其余都是真

### 实用技巧：
- 现代开发推荐使用 `Number.isFinite()` 和 `Number.isNaN()`，因为行为更可预测
- 记住口诀：「Number开头更严格，全局函数会转换」
- 测试时多用边界值：空字符串、null、undefined、0、NaN

这样分类记忆，就不会混淆这些函数的行为了！

<br>

# 3. JavaScript 中 null 和 undefined 的区别
在 JavaScript 中，`null` 和 `undefined` 都表示"空"或"无值"，但它们有重要的区别：

## 1. 定义和含义

### undefined
- **含义**：表示变量已声明但未赋值，或者访问不存在的属性
- **类型**：`undefined` 类型
- **出现场景**：
  - 声明了变量但未赋值
  - 函数没有返回值时的默认返回值
  - 访问对象不存在的属性
  - 函数参数未传递时的默认值

### null
- **含义**：表示"空值"或"无对象"，是一个明确的赋值
- **类型**：`object` 类型（这是 JavaScript 的一个历史遗留问题）
- **出现场景**：
  - 开发者主动赋值为 null
  - 表示变量应该包含对象，但目前为空

## 2. 代码示例

```javascript
// undefined 的情况
let a;
console.log(a); // undefined
console.log(typeof a); // "undefined"

let obj = {};
console.log(obj.name); // undefined

function test() {}
console.log(test()); // undefined

// null 的情况
let b = null;
console.log(b); // null
console.log(typeof b); // "object"

let person = null; // 明确表示 person 目前没有值
```

## 3. 比较操作

```javascript
// 相等比较
console.log(null == undefined);  // true (类型转换后相等)
console.log(null === undefined); // false (严格相等，类型不同)

// 布尔转换
console.log(Boolean(null));      // false
console.log(Boolean(undefined)); // false

// 数值转换
console.log(Number(null));       // 0
console.log(Number(undefined));  // NaN
```

## 4. 实际使用建议

### 使用 undefined
- 检查变量是否已初始化
- 检查对象属性是否存在
- 函数参数的默认值处理

```javascript
function greet(name) {
    if (name === undefined) {
        name = 'Guest';
    }
    console.log(`Hello, ${name}!`);
}
```

### 使用 null
- 明确表示"空值"
- 清空对象引用
- API 返回值表示"无数据"

```javascript
let user = getUserById(123);
if (user === null) {
    console.log('用户不存在');
}

// 清空引用
let element = document.getElementById('myDiv');
element = null; // 帮助垃圾回收
```

## 5. 检测方法

```javascript
// 检测 undefined
if (typeof variable === 'undefined') {
    // 变量未定义
}

// 检测 null
if (variable === null) {
    // 变量为 null
}

// 检测两者之一
if (variable == null) {
    // 变量为 null 或 undefined
}

// 现代方法：使用 nullish coalescing operator
let value = someVariable ?? 'default value';
```

## 总结

- **undefined**：系统级的"空值"，表示缺少值
- **null**：程序级的"空值"，表示明确的空值赋值
- 在实际开发中，建议使用 `null` 来明确表示"空值"，让 `undefined` 保持其"未定义"的语义
- 使用严格相等（`===`）来区分它们
- 可以使用 `??` 操作符来处理 null 和 undefined 的默认值情况

<br>

# 4. Number.MAX_SAFE_INTEGER、Number.MIN_SAFE_INTEGER、Number.MAX_VALUE、Number.MIN_VALUE 有什么区别？哪个大哪个小，如何区分？

## 1. 四个常量的基本概念
### Number.MAX_SAFE_INTEGER (最大安全整数)
- 数值 ： 9007199254740991
- 用途 ：表示 JavaScript 中能够 精确表示和计算 的最大整数
- 超出后果 ：整数运算可能失去精度
### Number.MIN_SAFE_INTEGER (最小安全整数)
- 数值 ： -9007199254740991
- 用途 ：表示 JavaScript 中能够 精确表示和计算 的最小整数
- 超出后果 ：整数运算可能失去精度
### Number.MAX_VALUE (最大数值)
- 数值 ： 1.7976931348623157e+308
- 用途 ：表示 JavaScript 中能够表示的 最大数值 （包括小数）
- 超出后果 ：返回 Infinity
### Number.MIN_VALUE (最小正数)
- 数值 ： 5e-324
- 用途 ：表示 JavaScript 中能够表示的 最小正数 （接近 0 但不等于 0）
- 注意 ：这不是最小负数，而是最小的正数！      

```javascript
MIN_SAFE_INTEGER < MIN_VALUE < MAX_SAFE_INTEGER < MAX_VALUE
-9007199254740991 < 5e-324 < 9007199254740991 < 1.7976931348623157e+308
```

| 常量               | 针对范围 | 主要用途           | 超出后果      |
| ------------------ | -------- | ------------------ | ------------- |
| `MAX_SAFE_INTEGER` | 整数     | 安全整数运算的上限 | 精度丢失      |
| `MIN_SAFE_INTEGER` | 整数     | 安全整数运算的下限 | 精度丢失      |
| `MAX_VALUE`        | 所有数值 | 数值表示的上限     | 返回 Infinity |
| `MIN_VALUE`        | 正数     | 最小正数值         | 更小会变成 0  |

