下面给你一套完整的 **JavaScript 强制类型转换与运算 500 题题库**，重点覆盖：

- **抽象值操作**：`ToPrimitive`、`ToNumber`、`ToString`、`ToBoolean`、`ToPropertyKey`
- **显式强制类型转换**：`Number()`、`String()`、`Boolean()`、`BigInt()` 等
- **隐式强制类型转换**：`+`、`-`、`*`、`/`、`%`、比较、位运算、逻辑运算
- **宽松相等**：`==`
- **严格相等**：`===`
- **综合运算**：多次转换叠加、对象转换、数组转换、`null` / `undefined` / `NaN` / `Infinity` / `-0`
- **面试高频陷阱**

建议做题方式：**先只写最终结果，再解释“发生了什么类型转换”**。

---

# 一、抽象值操作：ToPrimitive / ToNumber / ToString / ToBoolean / ToPropertyKey（1～100）

### 1～20：ToBoolean

1. `Boolean(false)` 的结果是什么？
2. `Boolean(true)` 的结果是什么？
3. `Boolean(0)` 的结果是什么？
4. `Boolean(-0)` 的结果是什么？
5. `Boolean(1)` 的结果是什么？
6. `Boolean(-1)` 的结果是什么？
7. `Boolean(NaN)` 的结果是什么？
8. `Boolean(Infinity)` 的结果是什么？
9. `Boolean(-Infinity)` 的结果是什么？
10. `Boolean("")` 的结果是什么？
11. `Boolean("0")` 的结果是什么？
12. `Boolean("false")` 的结果是什么？
13. `Boolean(" ")` 的结果是什么？
14. `Boolean(null)` 的结果是什么？
15. `Boolean(undefined)` 的结果是什么？
16. `Boolean([])` 的结果是什么？
17. `Boolean({})` 的结果是什么？
18. `Boolean([0])` 的结果是什么？
19. `Boolean([false])` 的结果是什么？
20. `Boolean(new Boolean(false))` 的结果是什么？

### 21～40：ToNumber

21. `Number("")` 的结果是什么？
22. `Number(" ")` 的结果是什么？
23. `Number("0")` 的结果是什么？
24. `Number("01")` 的结果是什么？
25. `Number("1.5")` 的结果是什么？
26. `Number("-1.5")` 的结果是什么？
27. `Number("1e3")` 的结果是什么？
28. `Number("0x10")` 的结果是什么？
29. `Number("0b1010")` 的结果是什么？
30. `Number("0o10")` 的结果是什么？
31. `Number("10px")` 的结果是什么？
32. `Number("1_000")` 的结果是什么？
33. `Number("Infinity")` 的结果是什么？
34. `Number("-Infinity")` 的结果是什么？
35. `Number("NaN")` 的结果是什么？
36. `Number(null)` 的结果是什么？
37. `Number(undefined)` 的结果是什么？
38. `Number(true)` 的结果是什么？
39. `Number(false)` 的结果是什么？
40. `Number([])` 的结果是什么？

### 41～60：ToString

41. `String(undefined)` 的结果是什么？
42. `String(null)` 的结果是什么？
43. `String(true)` 的结果是什么？
44. `String(false)` 的结果是什么？
45. `String(0)` 的结果是什么？
46. `String(-0)` 的结果是什么？
47. `String(1)` 的结果是什么？
48. `String(-1.5)` 的结果是什么？
49. `String(NaN)` 的结果是什么？
50. `String(Infinity)` 的结果是什么？
51. `String([])` 的结果是什么？
52. `String([1])` 的结果是什么？
53. `String([1,2])` 的结果是什么？
54. `String([null])` 的结果是什么？
55. `String([undefined])` 的结果是什么？
56. `String([[1,2]])` 的结果是什么？
57. `String({})` 的结果是什么？
58. `String(new Number(10))` 的结果是什么？
59. `String(new Boolean(false))` 的结果是什么？
60. `String(function(){})` 的结果是什么？

### 61～80：ToPrimitive

61. `String([])` 为什么不是 `"[object Array]"`？
62. `Number([])` 的转换过程是什么？
63. `Number([10])` 的转换过程是什么？
64. `Number([10,20])` 为什么不是 `1020`？
65. `Number({})` 的结果是什么？
66. `"x" + []` 的结果是什么？
67. `"x" + [1]` 的结果是什么？
68. `"x" + [1,2]` 的结果是什么？
69. `1 + []` 的结果是什么？
70. `1 + [2]` 的结果是什么？
71. `1 + [2,3]` 的结果是什么？
72. `1 + {}` 的结果是什么？
73. `{} + []` 在不同上下文中为什么可能产生不同结果？
74. `[] + {}` 的结果是什么？
75. `[1] + [2]` 的结果是什么？
76. `[1,2] + [3,4]` 的结果是什么？
77. `new Number(10) + 5` 的结果是什么？
78. `new Boolean(false) + 1` 的结果是什么？
79. `new String("10") * 2` 的结果是什么？
80. `new Number(10) == 10` 的结果是什么？

### 81～100：对象的 valueOf / toString / Symbol.toPrimitive

81. `const o={valueOf(){return 10}}; o+5` 结果是什么？
82. `const o={toString(){return "10"}}; o+5` 结果是什么？
83. `const o={valueOf(){return 10},toString(){return "20"}}; o+5` 结果是什么？
84. `const o={valueOf(){return "10"},toString(){return "20"}}; o-5` 结果是什么？
85. 为什么 `+o` 和 `o+""` 可能调用不同的转换路径？
86. `const o={Symbol.toPrimitive(){return "100"}}; o+1` 结果是什么？
87. `const o={Symbol.toPrimitive(){return 100}}; o+1` 结果是什么？
88. `const o={Symbol.toPrimitive(){return 100}}; String(o)` 结果是什么？
89. `const o={Symbol.toPrimitive(){return 100}}; Number(o)` 结果是什么？
90. `const o={valueOf(){return {}} ,toString(){return "8"}}; Number(o)` 结果是什么？
91. `const o={valueOf(){return {}} ,toString(){return {}}}; Number(o)` 会发生什么？
92. `const o={Symbol.toPrimitive(){return {}}}; Number(o)` 会发生什么？
93. `const o={Symbol.toPrimitive(){return 1}}; o==1` 为什么可能成立？
94. `const o={Symbol.toPrimitive(){return "1"}}; o==="1"` 为什么不成立？
95. `const o={Symbol.toPrimitive(){return "1"}}; o=="1"` 的结果是什么？
96. `const o={valueOf(){return 0}}; Boolean(o)` 为什么不是 `false`？
97. `const o={valueOf(){return 0}}; o ? 1 : 2` 的结果是什么？
98. `const o={toString(){return "0"}}; Number(o)` 的结果是什么？
99. `const o={valueOf(){return 10}}; String(o)` 的结果是什么？
100.  写出 `Object → Primitive → Number` 的完整转换链。

---

# 二、显式强制类型转换（101～220）

## 101～130：Number()

101. `Number("123")`
102. `Number("123.45")`
103. `Number("-123")`
104. `Number("+123")`
105. `Number("0")`
106. `Number("-0")`
107. `Number("")`
108. `Number("   ")`
109. `Number("\n\t")`
110. `Number("123abc")`
111. `Number("abc123")`
112. `Number("0xFF")`
113. `Number("0b1111")`
114. `Number("0o17")`
115. `Number("1e2")`
116. `Number("1e-2")`
117. `Number(".5")`
118. `Number("5.")`
119. `Number("+Infinity")`
120. `Number("-Infinity")`
121. `Number(Infinity)`
122. `Number(NaN)`
123. `Number(null)`
124. `Number(undefined)`
125. `Number(true)`
126. `Number(false)`
127. `Number([])`
128. `Number([1])`
129. `Number([1,2])`
130. `Number([null])`

## 131～160：String()

131. `String(10)`
132. `String(-10)`
133. `String(0)`
134. `String(-0)`
135. `String(0.1)`
136. `String(1e10)`
137. `String(NaN)`
138. `String(Infinity)`
139. `String(null)`
140. `String(undefined)`
141. `String(true)`
142. `String(false)`
143. `String([])`
144. `String([1])`
145. `String([1,2,3])`
146. `String([null,undefined])`
147. `String([[1]])`
148. `String({})`
149. `String(new String("abc"))`
150. `String(new Number(10))`
151. `String(new Boolean(false))`
152. `String(Symbol("x"))`
153. `String(BigInt(100))`
154. `String(function(){})`
155. `String(/abc/)`
156. `String(new Date(0))`
157. `String(Object.create(null))` 会怎样？
158. `String({toString(){return "hello"}})`
159. `String({valueOf(){return 10}})`
160. `String({[Symbol.toPrimitive](){return 99}})`

## 161～190：Boolean()

161. `Boolean(0)`
162. `Boolean(-0)`
163. `Boolean(0n)`
164. `Boolean(1n)`
165. `Boolean(NaN)`
166. `Boolean(Infinity)`
167. `Boolean("")`
168. `Boolean("0")`
169. `Boolean("false")`
170. `Boolean("null")`
171. `Boolean("undefined")`
172. `Boolean(" ")`
173. `Boolean(null)`
174. `Boolean(undefined)`
175. `Boolean([])`
176. `Boolean([0])`
177. `Boolean([false])`
178. `Boolean({})`
179. `Boolean(new Number(0))`
180. `Boolean(new Boolean(false))`
181. `Boolean(new String(""))`
182. `Boolean(Symbol())`
183. `Boolean(1/0)`
184. `Boolean(0/0)`
185. `Boolean(-Infinity)`
186. `Boolean("NaN")`
187. `Boolean("undefined")`
188. `Boolean([null])`
189. `Boolean([undefined])`
190. `Boolean(Object.create(null))`

## 191～220：一元 `+`、一元 `-`、BigInt、parseInt

191. `+"123"`
192. `+"123.5"`
193. `+""`
194. `+" "`
195. `+"abc"`
196. `+null`
197. `+undefined`
198. `+true`
199. `+false`
200. `+[]`
201. `+[1]`
202. `+[1,2]`
203. `+{}`
204. `-"123"`
205. `-null`
206. `-true`
207. `-false`
208. `-[]`
209. `-[1]`
210. `-[1,2]`
211. `parseInt("10")`
212. `parseInt("10px")`
213. `parseInt("10.5")`
214. `parseInt("0x10")`
215. `parseInt("010")`
216. `parseInt("1e3")`
217. `parseFloat("10.5px")`
218. `parseFloat("10abc")`
219. `BigInt("100")`
220. `BigInt(100)`

---

# 三、隐式强制类型转换：`+ - * / %`（221～320）

## 221～250：+

221. `1 + 2`
222. `1 + "2"`
223. `"1" + 2`
224. `"1" + true`
225. `"1" + false`
226. `"1" + null`
227. `"1" + undefined`
228. `"1" + NaN`
229. `"1" + Infinity`
230. `1 + true`
231. `1 + false`
232. `1 + null`
233. `1 + undefined`
234. `1 + NaN`
235. `1 + Infinity`
236. `true + true`
237. `true + false`
238. `false + false`
239. `null + null`
240. `null + undefined`
241. `undefined + undefined`
242. `[] + []`
243. `[] + {}`
244. `{} + []`
245. `[1] + [2]`
246. `[1,2] + [3,4]`
247. `[1] + 2`
248. `[1,2] + 2`
249. `[null] + 1`
250. `[undefined] + 1`

## 251～280：-

251. `1 - "1"`
252. `"10" - "3"`
253. `"10" - "3px"`
254. `10 - true`
255. `10 - false`
256. `10 - null`
257. `10 - undefined`
258. `10 - NaN`
259. `"10" - null`
260. `"10" - true`
261. `"10" - false`
262. `true - false`
263. `false - true`
264. `null - null`
265. `undefined - undefined`
266. `[] - []`
267. `[10] - 1`
268. `[10,20] - 1`
269. `["10"] - 1`
270. `["10","20"] - 1`
271. `"" - 1`
272. `" " - 1`
273. `"0" - []`
274. `"0" - [0]`
275. `"0" - [1]`
276. `"5" - null`
277. `"5" - undefined`
278. `"5" - false`
279. `"5" - true`
280. `"5" - NaN`

## 281～300：\*

281. `"2" * 3`
282. `"2" * "3"`
283. `"2px" * 3`
284. `true * 5`
285. `false * 5`
286. `null * 5`
287. `undefined * 5`
288. `NaN * 5`
289. `Infinity * 0`
290. `Infinity * 2`
291. `[] * 2`
292. `[2] * 2`
293. `[2,3] * 2`
294. `"" * 2`
295. `" " * 2`
296. `"5" * null`
297. `"5" * true`
298. `"5" * false`
299. `"5" * undefined`
300. `"5" * NaN`

## 301～320：/ 与 %

301. `"10" / 2`
302. `"10" / "2"`
303. `"10" / "0"`
304. `10 / "0"`
305. `0 / 0`
306. `0 / null`
307. `null / 0`
308. `1 / false`
309. `1 / true`
310. `1 / undefined`
311. `Infinity / Infinity`
312. `Infinity / 10`
313. `-Infinity / 10`
314. `10 % 3`
315. `"10" % "3"`
316. `"10" % 0`
317. `10 % 0`
318. `10 % null`
319. `10 % undefined`
320. `Infinity % 10`

---

# 四、隐式强制类型转换：比较运算（321～360）

321. `5 > "3"`
322. `"5" > 3`
323. `"10" > "2"`
324. `"2" > "10"`
325. `10 >= "10"`
326. `"10" >= 10`
327. `"10" < 20`
328. `"20" < "3"`
329. `true > false`
330. `true > 0`
331. `false > 0`
332. `null > 0`
333. `null >= 0`
334. `null < 0`
335. `undefined > 0`
336. `undefined < 0`
337. `NaN > 0`
338. `NaN < 0`
339. `NaN >= 0`
340. `"10" > null`
341. `"10" > undefined`
342. `[] > 0`
343. `[1] > 0`
344. `[2] > 1`
345. `[1,2] > 1`
346. `{}` > 0
347. `[] < "1"`
348. `[2] < "10"`
349. `"2" < "10"`
350. `2 < "10"`
351. `true < "2"`
352. `false < "1"`
353. `null < "1"`
354. `null < 1`
355. `undefined < 1`
356. `" "` < 1
357. `"" < 1`
358. `[] == 0` 和 `[] > 0` 是否有相同原因？
359. `[1] == 1` 和 `[1] > 0` 的转换过程有什么不同？
360. `"10" < 2` 和 `"10" < "2"` 为什么结果不同？

---

# 五、隐式强制类型转换：逻辑运算与位运算（361～390）

361. `0 || 100`
362. `1 || 100`
363. `"" || "hello"`
364. `"0" || "hello"`
365. `null || 100`
366. `undefined || 100`
367. `NaN || 100`
368. `[] || 100`
369. `{} || 100`
370. `false || "x"`
371. `true || "x"`
372. `0 && 100`
373. `1 && 100`
374. `"" && "x"`
375. `"0" && "x"`
376. `null && 100`
377. `undefined && 100`
378. `[] && 100`
379. `{} && 100`
380. `true && "hello"`
381. `!0`
382. `!"0"`
383. `![]`
384. `!{}`
385. `!!0`
386. `!!"0"`
387. `~~"10"`
388. `~~"10.8"`
389. `~~true`
390. `~~null`

---

# 六、宽松相等 `==`（391～440）

这是本套题的重点。

391. `1 == 1`

392. `1 == "1"`

393. `"1" == 1`

394. `true == 1`

395. `false == 0`

396. `true == "1"`

397. `false == "0"`

398. `null == null`

399. `undefined == undefined`

400. `null == undefined`

401. `null == 0`

402. `undefined == 0`

403. `null == false`

404. `undefined == false`

405. `null == ""`

406. `undefined == ""`

407. `NaN == NaN`

408. `NaN == "NaN"`

409. `Infinity == "Infinity"`

410. `-0 == 0`

411. `[] == []`

412. `[] == ""`

413. `[] == 0`

414. `[] == false`

415. `[0] == 0`

416. `[1] == 1`

417. `["1"] == 1`

418. `[""] == 0`

419. `[null] == 0`

420. `[undefined] == 0`

421. `[1,2] == "1,2"`

422. `[1,2] == 12`

423. `[1,2] == false`

424. `{} == {}`

425. `{} == "[object Object]"`

426. `new Number(1) == 1`

427. `new Boolean(false) == false`

428. `new String("x") == "x"`

429. `new Number(0) == false`

430. `new Boolean(false) == 0`

431. `"0" == false`

432. `"1" == true`

433. `" " == false`

434. `"" == false`

435. `"0" == 0`

436. `" " == 0`

437. `"\t" == 0`

438. `"01" == 1`

439. `"1e2" == 100`

440. `"0x10" == 16`

---

# 七、严格相等 `===`（441～470）

441. `1 === 1`

442. `1 === "1"`

443. `true === 1`

444. `false === 0`

445. `null === undefined`

446. `null === null`

447. `undefined === undefined`

448. `NaN === NaN`

449. `0 === -0`

450. `Infinity === Infinity`

451. `[] === []`

452. `[] === ""`

453. `[] === 0`

454. `[1] === [1]`

455. `{ } === { }`

456. `new Number(1) === 1`

457. `new Number(1) === new Number(1)`

458. `new String("x") === "x"`

459. `new Boolean(false) === false`

460. `Symbol("x") === Symbol("x")`

461. `const s = Symbol("x"); s === s`

462. `const a = {}; const b = a; a === b`

463. `const a = []; const b = a; a === b`

464. `Object(1) === 1`

465. `Object(1) === Object(1)`

466. `BigInt(10) === 10`

467. `BigInt(10) === 10n`

468. `10n === 10n`

469. `"10" === 10`

470. `"true" === true`

---

# 八、`==` 与 `===` 对比题（471～490）

471. 比较：`1 == "1"` 与 `1 === "1"`

472. 比较：`true == 1` 与 `true === 1`

473. 比较：`false == 0` 与 `false === 0`

474. 比较：`null == undefined` 与 `null === undefined`

475. 比较：`[] == 0` 与 `[] === 0`

476. 比较：`[1] == 1` 与 `[1] === 1`

477. 比较：`{} == {}` 与 `{} === {}`

478. 比较：`NaN == NaN` 与 `NaN === NaN`

479. 比较：`0 == -0` 与 `0 === -0`

480. 比较：`"0" == false` 与 `"0" === false`

481. 比较：`"" == false` 与 `"" === false`

482. 比较：`" " == false` 与 `" " === false`

483. 比较：`null == 0` 与 `null === 0`

484. 比较：`undefined == 0` 与 `undefined === 0`

485. 比较：`new Number(1) == 1` 与 `new Number(1) === 1`

486. 比较：`new Boolean(false) == false` 与 `new Boolean(false) === false`

487. 比较：`new String("1") == "1"` 与 `new String("1") === "1"`

488. 比较：`10n == 10` 与 `10n === 10`

489. 比较：`10n == "10"` 与 `10n === "10"`

490. 总结：`==` 与 `===` 最大的本质区别是什么？

---

# 九、综合强制转换题（491～500）

最后 10 道是面试级综合题。

### 491

```javascript
console.log([] == ![]);
```

求结果，并写出完整转换过程。

### 492

```javascript
console.log([] + {} == "[object Object]");
```

求结果。

### 493

```javascript
console.log("5" + 3 - 2);
```

求结果，并说明每一步类型。

### 494

```javascript
console.log(1 + true + "2" + false);
```

求结果。

### 495

```javascript
console.log("10" - "2" + "3");
```

求结果。

### 496

```javascript
console.log([] + [] + 1);
```

求结果。

### 497

```javascript
console.log([1] + [2] + [3]);
```

求结果。

### 498

```javascript
console.log(true + false + "1" + null);
```

求结果。

### 499

```javascript
console.log(null == undefined && null === undefined);
```

求结果。

### 500

```javascript
console.log([] == 0 && [] === 0 && "0" == false && "0" === false);
```

求最终结果，并解释四个比较分别发生了什么。

---

# 这 500 道题的知识结构

你可以把这套题理解成下面这条学习路线：

```text
JavaScript 类型转换
        │
        ├── 1. 抽象值操作
        │      ├── ToPrimitive
        │      ├── ToNumber
        │      ├── ToString
        │      ├── ToBoolean
        │      └── ToPropertyKey
        │
        ├── 2. 显式强制转换
        │      ├── Number()
        │      ├── String()
        │      ├── Boolean()
        │      ├── BigInt()
        │      ├── parseInt()
        │      └── parseFloat()
        │
        ├── 3. 隐式强制转换
        │      ├── +
        │      ├── -
        │      ├── *
        │      ├── /
        │      ├── %
        │      ├── >
        │      ├── <
        │      ├── &&
        │      ├── ||
        │      └── !
        │
        ├── 4. 宽松相等
        │      └── ==
        │
        ├── 5. 严格相等
        │      └── ===
        │
        └── 6. 综合题
               ├── null
               ├── undefined
               ├── NaN
               ├── Infinity
               ├── -0
               ├── 数组
               ├── 对象
               ├── 包装对象
               └── Symbol.toPrimitive
```

其中最值得你重点刷的是这几类：

```javascript
[] == 0
[] == false
[1] == 1
[1,2] == "1,2"

"5" + 2
"5" - 2

true + true
false + 1

null + 1
undefined + 1

null == undefined
null == 0

NaN == NaN
NaN === NaN

0 == -0
0 === -0

10n == 10
10n === 10

{} + []
[] + {}
```
