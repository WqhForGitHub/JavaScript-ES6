// 改前：打开登录浮层的函数里混进了埋点上报，业务代码和统计代码搅在一起

function showLogin(source: string): void {
  // ---------- 业务逻辑：打开浮层 ----------
  console.log(`打开登录浮层（来源：${source}）`);

  // ---------- 埋点上报：产品经理要统计曝光 ----------
  console.log('[埋点] login_layer_expose +1');
  // 来源渠道上报
  console.log(`[埋点] login_source = ${source}`);
  // 以后要加"上报打开耗时""上报 A/B 实验分组"……只能继续往这里塞
}

// ========== 各入口调用 ==========
showLogin('首页顶栏');
showLogin('商品详情页');

// 问题：
// 1. 业务函数一半的代码在干统计的活，想读懂"登录浮层怎么打开"要在埋点代码里跳着看
// 2. 想替换统计方案（自研切到神策）或临时下线埋点，必须修改业务函数
// 3. 埋点代码无法复用：另一个按钮也要曝光埋点，只能复制粘贴一份
// 4. 埋点代码若抛异常，会连累登录功能一起挂掉，故障半径被无谓放大

export {};
